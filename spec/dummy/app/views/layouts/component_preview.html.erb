<!DOCTYPE html>
<html class="<%= params[:lookbook][:display][:bg_color] || "bg-white" %>">
<head>
  <title>Component Preview</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">

  <%= stylesheet_link_tag "alveole", "data-turbo-track": "reload" %>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.js"></script>

  <%= javascript_importmap_tags "alveole/application" %>

  <!--
    NOTE: To add engine's javascript functionality we have to load the
          entrypoint here or `import` it in the main app `application.js`
  -->
  <%#= javascript_import_module_tag "alveole/application" %>
</head>
<body style="padding: 20px">
  <div style="
    margin-left: auto;
    margin-right: auto;
    max-width: <%= params[:lookbook][:display][:max_width] || "100%" %>
  ">
    <%= yield %> <!-- rendered preview will be injected here -->
  </div>
</body>
</html>
