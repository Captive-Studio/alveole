<alv-sidebar-heading class="flex h-16 shrink-0 items-center mb-6">
  <%= image_tag @image, class: "h-8 w-auto" if @image %>
  <% if @title %>
    <span><%= @title %></span>
  <% end %>
</alv-sidebar-heading>
